apiVersion: v1
kind: Service
metadata:
  name: "{{ .Values.ui.name }}-service"
spec:
  selector:
    app: {{ .Values.ui.name }}
  ports:
    - protocol: TCP
      port: {{ .Values.ui.port }}
      targetPort: {{ .Values.ui.targetPort }}
